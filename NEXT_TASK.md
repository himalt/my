# NEXT_TASK

优先执行 `LOCAL_CLIENT_MVP_PLAN.md`。

## 当前第一目标

从 `LOCAL_CLIENT_MVP_PLAN.md` 的阶段一和阶段二开始：

1. 检查项目启动入口。
2. 检查数据库路径。
3. 检查图片保存路径。
4. 检查商品处理页接口。
5. 检查上货 RPA 入口。
6. 确认哪些数据已经本地化，哪些还依赖服务器或外链。
7. 输出最小改造计划。
8. 开始修改本地数据目录和图片目录逻辑。
9. 运行最小验证，确认本地页面、SQLite、图片目录、日志目录可用。

## 执行约束

- 不要先做登录。
- 不要做计费。
- 不要做 CDN。
- 不要做对象存储。
- 不要重写整个系统。
- 优先复用现有 FastAPI + SQLite + 前端代码。
- 优先保证 `采集 → 图片处理 → 商品处理 → 上货 → 结果回写` 闭环。
- 不要优先美化 UI。
- 能自动修复和验证的，不要询问用户。
- 除非遇到账号、验证码、真实上货确认、外部 API 密钥、支付配置，否则继续自主推进。

## 可直接给 AI 的启动指令

```text
你现在接手 D:\projects\upload-assistant 项目。

请先读取：
1. D:\projects\memories\PROFILE.md
2. D:\projects\memories\ACTIVE.md
3. D:\projects\upload-assistant\LOCAL_CLIENT_MVP_PLAN.md
4. D:\projects\upload-assistant\NEXT_TASK.md

然后按照 LOCAL_CLIENT_MVP_PLAN.md 和 NEXT_TASK.md 执行。

目标：
把现有网页系统改造成“本地 Web 客户端 MVP”。

核心闭环必须包括：
采集商品 → 下载/处理图片 → 商品处理 → 上货 RPA → 成功/失败回写 → 失败重试 → 导出/清理。

执行规则：
- 不要先做登录。
- 不要做计费。
- 不要做 CDN。
- 不要做对象存储。
- 不要重写整个系统。
- 优先复用现有 FastAPI + SQLite + 前端代码。
- 优先保证功能闭环，不优先美化 UI。
- 能自动修复和验证的，就不要问我。
- 除非遇到账号、验证码、真实上货确认、外部 API 密钥、支付配置，否则继续自主推进。
- 每轮先制定计划，再修改代码，再运行最小验证。
- 最后汇报：完成了什么、改了哪些文件、验证结果、还剩什么风险。

先从“阶段一：本地运行基础”和“阶段二：采集入库”开始。
```
